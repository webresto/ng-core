import { ModifiresDirective } from './modifires.directive';

describe('ModifiresDirective', () => {
  it('should create an instance', () => {
    const directive = ModifiresDirective;
    expect(directive).toBeTruthy();
  });
});
